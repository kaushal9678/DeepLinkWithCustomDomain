/* import { create } from 'apisauce'



// define the api
const api = create({
  baseURL: 'https://asia-south1-socialape-b910e.cloudfunctions.net/api',
  headers: { Accept: 'application/json' },
})

// start making calls
export const getScreams=async(param:string)=>{
  const response = await api.get(param);
  return response.data;
}


// customizing headers per-request
//api.post('/users', { name: 'steve' }, { headers: { 'x-gigawatts': '1.21' } })
 */